#include <bits/stdc++.h>
#define ll long long
using namespace std;


int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
   
    ll int x{};
    cin >> x;
    string a;
    string word;
    ll int res{};
    ll int count{};
    for(int i = 0; i < x; i++){
        cin  >> word;
        a+=word;
    }
    int b = 0;
    
    for(int k = 0; k < a.size(); k++){
        if()
    }



    return 0;
}