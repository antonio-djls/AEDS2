#include <bits/stdc++.h>
#define ll long long
using namespace std;


int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll int x{}; cin >> x;
    ll int input{};
    while(x--){
        cin >> input;
        for(int i = 0; i < 10; i++){
            if()
        }

    }

    return 0;
}
