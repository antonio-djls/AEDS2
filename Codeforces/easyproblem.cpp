#include <bits/stdc++.h>
#define ll long long
using namespace std;


int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll int x{}; cin >> x;
    ll int input{};
    for(int  u = 0; u <x ; u++){
        cin >> input;
        cout << input-1 << endl;

    }

    return 0;
}
